const AuthUtils = {};

/**
 * createToken
 * @param {String} id
 * @returns {String} token string
 */
AuthUtils.createToken = function(id) {
    const token = 'hello';
    return token;    
}

module.exports = AuthUtils;